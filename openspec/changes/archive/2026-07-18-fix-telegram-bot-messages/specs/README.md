## Specs

No new or modified capabilities for this change. This change fixes bugs in existing behavior (handler routing, input parsing) and updates user-facing documentation (help text, command descriptions). No spec-level behavior changes required.
